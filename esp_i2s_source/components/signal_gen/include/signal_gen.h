/*
 * signal_gen — sine / sweep / silence producers at 44.1 kHz stereo s16
 * (SPEC §4). Pure, host-testable sample math; the audio path's built-in
 * diagnostic and the Phase-1 source.
 *
 * Implemented in SIG-1a. Skeleton only for now.
 */
#pragma once
