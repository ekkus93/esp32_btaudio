# radio — HTTP(S) stream client + esp_audio_codec decode (MP3/AAC/HE-AACv2)
# + resample to 44.1 kHz (RADIO-1/RADIO-2). Header-only skeleton until RADIO-1.
idf_component_register(INCLUDE_DIRS "include")
