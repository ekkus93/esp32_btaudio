/* Forwarder to share the production audio processor interface without duplicating
 * test-only declarations. This mirrors the forwarding header used by the
 * Bluetooth test app so both builds stay aligned with the canonical definitions
 * in main/include.
 */

#ifndef TEST_APP_AUDIO_AUDIO_PROCESSOR_FORWARDER_H
#define TEST_APP_AUDIO_AUDIO_PROCESSOR_FORWARDER_H

#include "../../../../components/audio_processor/include/audio_processor.h"

#endif /* TEST_APP_AUDIO_AUDIO_PROCESSOR_FORWARDER_H */
