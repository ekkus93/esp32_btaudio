This folder is packaged into the SPIFFS partition image via spiffs_create_partition_image(spiffs ...).

Contents:
- worker_long_norm.wav — sample asset included in the production image.

Add any additional files here; they will be flashed automatically with `idf.py build flash` when the spiffs partition is present in partitions.csv.
