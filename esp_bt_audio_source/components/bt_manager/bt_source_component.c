/*
 * Intentionally empty translation unit.
 * The bt_manager component provides the concrete bt_* APIs; this file
 * previously held duplicate wrappers that are now removed to avoid
 * redundant symbols and dead code.
 */
